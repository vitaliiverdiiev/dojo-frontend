export enum RoutesEnum {
  HOME = "/",
  ABOUT = "/about",
  RESUME = "/resume",
  SIGNIN = "/sign-in",
  SIGNUP = "/sign-up",
  NOT_FOUNT = "/not-found",
  BLOG_POSTS = "/blog-posts",
  COUNTER = "/counter",
  TODOS = "/todos",
}
